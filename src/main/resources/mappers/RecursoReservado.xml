<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="edu.eci.cvds.sampleprj.dao.mybatis.mappers.RecursoReservado">
    <insert id="insertarReservado" parameterType="map">
            INSERT INTO reservadoS 
            VALUES (
            #{reservado.id}, 
            #{reservado.fechainiciorenta},
            #{reservado.fechafinrenta},
            #{reservado.carrera},
            #{reservado.item.id}
            )
    </insert>

    <select parameterType="map" id="consultarReserva" resultMap="reservadoResult" >
        SELECT    
		*
   		FROM reservadoS AS r
        WHERE r.id=#{id}
    </select>

    
    <select parameterType="map" id="consultarReservas" resultMap="reservadoResult" >
    	SELECT    
       	*
        FROM reservadoS AS r 
    </select>
    
    <resultMap type='reservado' id='reservadoResult' >
        <id property='id' column='id'/>
        <result property='fechainiciorenta' column='fechainiciorenta'/>
        <result property='fechafinrenta' column='fechafinrenta'/>
        <association property="Recurso" javaType="Recurso" resultMap='edu.eci.cvds.sampleprj.dao.mybatis.mappers.RecursoMapper.RecursoResult'></association>
    </resultMap>
         
</mapper>